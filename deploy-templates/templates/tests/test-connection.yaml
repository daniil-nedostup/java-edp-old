apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edp-old.fullname" . }}-test-connection"
  labels:
    {{- include "edp-old.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edp-old.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
