apiVersion: v1
kind: Service
metadata:
  name: {{ include "edp-old.fullname" . }}
  labels:
    {{- include "edp-old.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edp-old.selectorLabels" . | nindent 4 }}
